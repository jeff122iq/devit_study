const array = require("../index")

// 9. Создать функцию которая нормализует данные в массиве исключая или преобразуя не подходящие.

// const arrayNormalize = (arr,schema,transform) => {
//   let res = arr.filter(element => {
//     if(typeof element === schema) {
//       console.log(element)
//     }
//   })
//   return res
// }
// console.log(arrayNormalize(array.testData4,{age:'float'}, Boolean = false))
